'use strict';

describe('BrewControl.status module', function() {

  beforeEach(module('BrewControl.status'));

  describe('status controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var infoCtrl = $controller('StatusCtrl');
      expect(infoCtrl).toBeDefined();
    }));

  });
});