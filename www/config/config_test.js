'use strict';

describe('BrewControl.config module', function() {

  beforeEach(module('BrewControl.config'));

  describe('config controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var configCtrl = $controller('ConfigCtrl');
      expect(configCtrl).toBeDefined();
    }));

  });
});